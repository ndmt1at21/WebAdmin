"use strict";

var _require = require('child_process'),
    spawn = _require.spawn;

var rebuildIfDarwin = function rebuildIfDarwin() {
  if (process.platform !== 'darwin') {
    console.log();
    console.log("Skipping 'fsevents' build as platform ".concat(process.platform, " is not supported"));
    process.exit(0);
  } else {
    spawn('node-gyp', ['rebuild'], {
      stdio: 'inherit'
    });
  }
};

rebuildIfDarwin();
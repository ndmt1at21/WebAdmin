'use strict';

var path = require('path');

var fs = require('fs');

var hasYarn = function hasYarn() {
  var cwd = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : process.cwd();
  return fs.existsSync(path.resolve(cwd, 'yarn.lock'));
};

module.exports = hasYarn; // TODO: Remove this for the next major release

module.exports["default"] = hasYarn;
"use strict";

var normalize = require('../');

var t = require('tap');

t.test('benign array', function _callee(t) {
  var pkg, expect;
  return regeneratorRuntime.async(function _callee$(_context) {
    while (1) {
      switch (_context.prev = _context.next) {
        case 0:
          pkg = {
            name: 'hello',
            version: 'world',
            bin: ['./x/y', 'y/z', './a']
          };
          expect = {
            name: 'hello',
            version: 'world',
            bin: {
              y: 'x/y',
              z: 'y/z',
              a: 'a'
            }
          };
          t.strictSame(normalize(pkg), expect);
          t.strictSame(normalize(normalize(pkg)), expect, 'double sanitize ok');

        case 4:
        case "end":
          return _context.stop();
      }
    }
  });
});
t.test('conflicting array', function _callee2(t) {
  var pkg, expect;
  return regeneratorRuntime.async(function _callee2$(_context2) {
    while (1) {
      switch (_context2.prev = _context2.next) {
        case 0:
          pkg = {
            name: 'hello',
            version: 'world',
            bin: ['./x/y', 'z/y', './a']
          };
          expect = {
            name: 'hello',
            version: 'world',
            bin: {
              y: 'z/y',
              a: 'a'
            }
          };
          t.strictSame(normalize(pkg), expect);
          t.strictSame(normalize(normalize(pkg)), expect, 'double sanitize ok');

        case 4:
        case "end":
          return _context2.stop();
      }
    }
  });
});
t.test('slashy array', function _callee3(t) {
  var pkg, expect;
  return regeneratorRuntime.async(function _callee3$(_context3) {
    while (1) {
      switch (_context3.prev = _context3.next) {
        case 0:
          pkg = {
            name: 'hello',
            version: 'world',
            bin: ['/etc/passwd']
          };
          expect = {
            name: 'hello',
            version: 'world',
            bin: {
              passwd: 'etc/passwd'
            }
          };
          t.strictSame(normalize(pkg), expect);
          t.strictSame(normalize(normalize(pkg)), expect, 'double sanitize ok');

        case 4:
        case "end":
          return _context3.stop();
      }
    }
  });
});
t.test('dotty array', function _callee4(t) {
  var pkg, expect;
  return regeneratorRuntime.async(function _callee4$(_context4) {
    while (1) {
      switch (_context4.prev = _context4.next) {
        case 0:
          pkg = {
            name: 'hello',
            version: 'world',
            bin: ['../../../../etc/passwd']
          };
          expect = {
            name: 'hello',
            version: 'world',
            bin: {
              passwd: 'etc/passwd'
            }
          };
          t.strictSame(normalize(pkg), expect);
          t.strictSame(normalize(normalize(pkg)), expect, 'double sanitize ok');

        case 4:
        case "end":
          return _context4.stop();
      }
    }
  });
});
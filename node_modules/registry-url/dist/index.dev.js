'use strict';

var rc = require('rc');

var registryUrl = function registryUrl(scope) {
  var result = rc('npm', {
    registry: 'https://registry.npmjs.org/'
  });
  var url = result["".concat(scope, ":registry")] || result.config_registry || result.registry;
  return url.slice(-1) === '/' ? url : "".concat(url, "/");
};

module.exports = registryUrl; // TODO: Remove this for the next major release

module.exports["default"] = registryUrl;
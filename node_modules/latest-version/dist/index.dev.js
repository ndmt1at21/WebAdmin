'use strict';

var packageJson = require('package-json');

var lastestVersion = function lastestVersion(packageName, options) {
  var _ref, version;

  return regeneratorRuntime.async(function lastestVersion$(_context) {
    while (1) {
      switch (_context.prev = _context.next) {
        case 0:
          _context.next = 2;
          return regeneratorRuntime.awrap(packageJson(packageName.toLowerCase(), options));

        case 2:
          _ref = _context.sent;
          version = _ref.version;
          return _context.abrupt("return", version);

        case 5:
        case "end":
          return _context.stop();
      }
    }
  });
};

module.exports = lastestVersion; // TODO: Remove this for the next major release

module.exports["default"] = lastestVersion;
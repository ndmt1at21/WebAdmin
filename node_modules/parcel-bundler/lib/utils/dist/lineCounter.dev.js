"use strict";

function lineCounter(string) {
  var lines = 1;

  for (var i = 0; i < string.length; i++) {
    if (string.charAt(i) === '\n') {
      lines++;
    }
  }

  return lines;
}

module.exports = lineCounter;
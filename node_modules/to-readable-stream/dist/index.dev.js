'use strict';

var _require = require('stream'),
    Readable = _require.Readable;

module.exports = function (input) {
  return new Readable({
    read: function read() {
      this.push(input);
      this.push(null);
    }
  });
};